import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const General = () => {
  return (
    <View style={{flex: 1}}>
      <Text style={{color: 'blue'}}>General</Text>
    </View>
  );
};

export default General;

const styles = StyleSheet.create({});
